Person :: struct {
    name: i32,
    age: i32,
}

pub main :: fn() !void {
    // Test GC arrays - this should work
    gc_array :: $[1, 2, 3]
    // Test normal arrays - this should work
    normal_array :: [10, 20, 30]
    // Test GC struct - this should work
    gc_person :: $Person{ .name = 42, .age = 25 }
    return
}
// Test @fieldInfo built-in function

std :: @import("std")

Person :: struct {
    name: str,
    age: i32,
    email: str,
}

main :: fn () void {
    // Test basic fieldInfo usage
    name_info :: @fieldInfo(Person, "name")
    age_info :: @fieldInfo(Person, "age")
    email_info :: @fieldInfo(Person, "email")
    
    // This should compile successfully if @fieldInfo is properly implemented
    std.debug.print("Field info compiled successfully\n", .{})
}
// Simple test with error handling but avoiding struct returns
MyError :: error { 
    DivisionByZero,
    InvalidValue,
}

divide :: fn (a: i32, b: i32) MyError!i32 {
    return if (b == 0) MyError.DivisionByZero else (a / b)
}

main :: fn () void {
    x :: 42
    y :: 10
}
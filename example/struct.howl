std :: @import("std")

MyStruct :: struct {
    name: str,
    age: isize,

    myStrucFunc :: fn (a : MyStruct) isize {
        return a.age
    }

}

myFunc :: fn (a : MyStruct) void {
    return
}
